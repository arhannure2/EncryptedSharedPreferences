<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.encryptedsharedpreferences" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_security"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_security"
        android:supportsRtl="true"
        android:theme="@style/Theme.EncryptedSharedPreferences"
        android:name=".SecurityApp"
        >



        <activity android:name=".EncryptedFileActivity" />
        <activity android:name=".EncryptedPreferencesActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SampleActivity" />

        <activity android:name=".SplashScreenActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>